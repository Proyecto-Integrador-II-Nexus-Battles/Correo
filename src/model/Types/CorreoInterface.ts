export interface EmailInterface {
  info: {
    user: string;
    password: string;
  };
  promo: {
    user: string;
    password: string;
  };
  subasta: {
    user: string;
    password: string;
  };
}
